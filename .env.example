# Template only. Copy to .env and fill in real values. .env is gitignored.

# The agent under test
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# The judge (Box 9) - deliberately a different provider from the agent,
# so the judge cannot prefer its own family's answers.
JEV=apikey_...
CLOUDFLARE_ACCOUNT_ID=...
